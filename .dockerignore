# 빌드 컨텍스트는 저장소 루트다(infra/docker-compose.yml 의 context: ..).
# build/ 만 95MB 라 제외하지 않으면 매 빌드마다 그대로 전송된다.
.git
.gitignore
.gradle
build
out
bin
.idea
.vscode
.claude
.DS_Store
__MACOSX
*.log

# 런타임 데이터와 샘플 영상 — 이미지에 들어갈 이유가 없다.
data
videos

# 시크릿이 이미지 레이어에 남지 않도록 명시적으로 제외한다.
infra/.env
infra/certs
